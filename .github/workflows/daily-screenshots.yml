name: Daily Race Card Screenshots

# NOTE: TwinSpires blocks datacenter IPs — screenshot capture must run
# locally via Windows Task Scheduler (run-screenshots.bat at 7 AM).
# This workflow is disabled until a residential proxy is configured.

on:
  workflow_dispatch:  # manual trigger only for now
